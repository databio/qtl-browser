# raw and derived data never go in git; keep the download config and script
data/**
!data/raw/
!data/raw/sources.yaml
!data/raw/download.py

# secrets (bucket credentials for uploading the store)
.env

# Finder metadata
.DS_Store

# python env
.venv/
__pycache__/

# ui app
ui/node_modules/
ui/dist/
ui/bench/out/
ui/bench/results/

# R / RStudio session files
.Rhistory
.RData
.Rproj.user/
*.Rproj
